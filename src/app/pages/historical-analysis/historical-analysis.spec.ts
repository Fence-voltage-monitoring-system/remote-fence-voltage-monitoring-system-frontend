import{TestBed}from'@angular/core/testing';import{HistoricalAnalysis}from'./historical-analysis';describe('HistoricalAnalysis',()=>{it('creates the historical dashboard',async()=>{await TestBed.configureTestingModule({imports:[HistoricalAnalysis]}).compileComponents();const fixture=TestBed.createComponent(HistoricalAnalysis);fixture.detectChanges();expect(fixture.componentInstance.metrics.length).toBe(6);});});
