import{HttpClient,HttpParams}from'@angular/common/http';import{inject,Injectable}from'@angular/core';import{Observable}from'rxjs';import{AlertFilters,AlertPage,AlertRecord,AlertStats}from'../../pages/alerts/alerts.models';
@Injectable({providedIn:'root'})export class AlertService{private readonly http=inject(HttpClient);private readonly endpoint='/api/alerts';getAlerts(filters:AlertFilters,page=1,pageSize=20):Observable<AlertPage>{let params=new HttpParams().set('page',page).set('pageSize',pageSize);Object.entries(filters).forEach(([key,value])=>{if(value)params=params.set(key,value)});return this.http.get<AlertPage>(this.endpoint,{params,withCredentials:true});}getStats():Observable<AlertStats>{return this.http.get<AlertStats>(`${this.endpoint}/stats`,{withCredentials:true});}acknowledge(id:number):Observable<AlertRecord>{return this.http.patch<AlertRecord>(`${this.endpoint}/${id}/acknowledge`,{},{withCredentials:true});}addComment(id:number,comment:string):Observable<AlertRecord>{return this.http.post<AlertRecord>(`${this.endpoint}/${id}/comments`,{comment},{withCredentials:true});}assignMaintenance(id:number,staffId:number|null=null):Observable<AlertRecord>{return this.http.post<AlertRecord>(`${this.endpoint}/${id}/maintenance-assignments`,{staffId},{withCredentials:true});}connectLive():Observable<AlertRecord>{return new Observable(subscriber=>{const protocol=location.protocol==='https:'?'wss:':'ws:';const socket=new WebSocket(`${protocol}//${location.host}/api/alerts/ws`);socket.onmessage=event=>{try{subscriber.next(JSON.parse(event.data)as AlertRecord)}catch{subscriber.error(new Error('Invalid alert message.'))}};socket.onerror=()=>subscriber.error(new Error('Alert live connection failed.'));socket.onclose=()=>subscriber.complete();return()=>socket.close();});}}
